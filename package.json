{
  "name": "tialynn-workspace",
  "private": true,
  "version": "0.14.0",
  "type": "module",
  "description": "TiaLynn — Electron + Vue 3 桌面 AI 伴侣（Live2D + 本地 LLM + RVC 语音）",
  "scripts": {
    "dev": "pnpm -F tialynn-electron dev",
    "build": "pnpm -F tialynn-electron build",
    "typecheck": "pnpm -F tialynn-electron typecheck",
    "package:mac": "pnpm -F tialynn-electron package:mac",
    "package:win": "pnpm -F tialynn-electron package:win",
    "package:linux": "pnpm -F tialynn-electron package:linux"
  }
}
